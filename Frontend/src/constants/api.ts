export const API_BASE = "api"; // "/api"

export const AUTH_PREFIX = `${API_BASE}/auth`;
export const USERS_PREFIX = `${API_BASE}/users`;
export const TICKETS_PREFIX = `${API_BASE}/tickets`;
export const EVENT_PREFIX = `${API_BASE}/event`;
export const CATEGORY_PREFIX = `${API_BASE}/eventcategory`;
export const EVENT_ZONE_PREFIX = `${API_BASE}/eventzone`;
export const LOCAL_PREFIX  = `${API_BASE}/local`;
export const CITY_PREFIX  = `${API_BASE}/city`;
export const DISTRICT_PREFIX  = `${API_BASE}/district`;
export const ADMIN_CLIENT_PREFIX  = `${API_BASE}/admin/clients`;
export const BULK_PREFIX  = `${API_BASE}/bulk`;
export const LOCATION_ZONE_PREFIX  = `${API_BASE}/localzone`;

// Endpoints del Carrito (Dev 2)
export const CART_PREFIX = `${API_BASE}/cart`;

// Endpoints de Checkout (Dev 4)
export const CHECKOUT_PREFIX = `${API_BASE}/checkout`;

// Endpoints de Puntos de Lealtad (Dev 5)
export const LOYALTY_PREFIX = `${API_BASE}/loyalty`;

// Endpoints de Órdenes/Historial (Dev 5)
export const ORDERS_PREFIX = `${API_BASE}/orders`;

export const DASHBOARD_PREFIX  = `${API_BASE}/dashboard`;
export const EVENT_METRIC_PREFIX  = `${API_BASE}/eventsMetrics`;
export const CATEGORY_METRICS_PREFIX  = `${API_BASE}/categoryMetrics`;
export const METRICS_PREFIX  = `${API_BASE}/metrics`;
export const ADMIN_PREFIX = `${API_BASE}/admins`;
export const REPORT_PREFIX = `${API_BASE}/reports/dashboard`;
export const LOYALTY_ADMIN_PREFIX = `${API_BASE}/admin/loyalty`;


export const AUDIT_LOG_PREFIX = `${API_BASE}/audit-log`;
